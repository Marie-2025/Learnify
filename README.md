LEARNIFY MOBILE APP

Look for config.js in the root folder, Open it and update the  API_BASE_URL;

Open the command prompt and type "ipconfig" and the ip address u get is what you will use to update the API_BASE_URL
export const API_BASE_URL = 'http://<IP Address from command prompt>:{Port for your server}'; // Replace with your IP and port.